# Model: claude-sonnet-4-6

# Task 2a: Verify Both Report Writers Populate At-Filing Columns

## Phase

Wave 2 — only after wave-1-complete tag.

## Branch

`db-bloat/wave-2/task-2a-report-writers`

## Objective

Before dropping `unified_reports.raw_data`, verify both report writers set
`committee_name_at_filing` and `treasurer_name_at_filing` directly from source
columns — not via later JSONB backfill over `raw_data`.

## File Ownership

This task owns:
- `app/core/ingest_vectorized/families/reports.py` — vectorized writer fix if needed
- `app/core/source_models/reports.py` — field definitions if needed

## Context

There are two report writers:
1. **ORM writer**: `app/core/source_models/reports_ingest.py` (~L95-112)
   — Already sets `committee_name_at_filing=raw.get("filerName")` and
   `treasurer_name_at_filing=raw.get("treasurerNameFirst") + ...` at insert.
   Do NOT edit this file — it is owned by task 2b.

2. **Vectorized writer**: `app/core/ingest_vectorized/families/reports.py` (~L110/131)
   — **Verify**: does it set the at-filing columns, or only write `raw_data`?

## Implementation

### Step 1: Verify vectorized writer

Read `app/core/ingest_vectorized/families/reports.py`. Check whether it writes
`committee_name_at_filing` and `treasurer_name_at_filing` to the output frame.

If it does: no change needed, document the finding.

If it only writes `raw_data` (or neither at-filing column):
- Add the two derived columns to the output Polars frame
- Map them from the source parquet columns:
  - `committee_name_at_filing` ← source col for filer/committee name at filing date
  - `treasurer_name_at_filing` ← source col for treasurer name

Use Context7 (`resolve-library-id` → `get-library-docs`) for current Polars docs
before writing Polars expressions.

Run `gitnexus_impact` on `reports.py` `run()` before editing.

### Step 2: Confirm field definitions

Check `app/core/source_models/reports.py` (~L103) that `UnifiedReport` has:
- `committee_name_at_filing: Optional[str]`
- `treasurer_name_at_filing: Optional[str]`
- `raw_data: Optional[str]` (still present, will be dropped in 2b)

No changes needed to `reports.py` unless fields are missing.

## Commit

```
fix: ensure vectorized report writer populates at-filing cols (2a)
```

## Checklist

- [ ] Wave 1 `db-bloat/wave-1-complete` tag confirmed
- [ ] `gitnexus_impact` run on vectorized `reports.py` `run()`; no HIGH/CRITICAL
- [ ] Context7 consulted for Polars docs
- [ ] Verified both report writers set at-filing columns from source
- [ ] Fixed vectorized writer if it only wrote raw_data
- [ ] `gitnexus_detect_changes()` run before commit
