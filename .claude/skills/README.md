# Project Skills

Project-scoped Claude Code skills for campaignfinance.

The `project-alignment` skill (Abstract Data) was used to retrofit this repo to
the standard template. It is distributed via the Abstract Data plugin
marketplace rather than vendored here — install it from there to re-run
alignment audits. Add project-specific skills as subdirectories with a
`SKILL.md` each.
